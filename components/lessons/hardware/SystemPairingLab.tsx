import { useState } from "react";
import { canCompleteTask, classifySystemItem } from "@/lib/hardware-software-lesson";
const ITEMS = [{ id: "mouse", label: "鼠标", icon: "🖱️" }, { id: "screen", label: "屏幕", icon: "🖥️" }, { id: "paint-app", label: "画图程序", icon: "🎨" }, { id: "printer", label: "打印机", icon: "🖨️" }, { id: "print-app", label: "打印程序", icon: "📄" }, { id: "microphone", label: "麦克风", icon: "🎤" }, { id: "music-app", label: "录音程序", icon: "🎵" }];
const TASKS = [{ id: "draw", label: "画一幅画" }, { id: "print", label: "打印作品" }, { id: "record", label: "录一段声音" }];
interface Props { onComplete: () => void; }
export function SystemPairingLab({ onComplete }: Props) { const [index, setIndex] = useState(0); const [selected, setSelected] = useState<string[]>([]); const [feedback, setFeedback] = useState("同时选择任务需要的硬件和软件。" ); const task = TASKS[index]; function check() { if (!canCompleteTask(task.id, selected)) { setFeedback("还缺搭档：检查是否既有能摸到的硬件，也有负责指令的软件。" ); return; } if (index === TASKS.length - 1) onComplete(); else { setIndex((value) => value + 1); setSelected([]); setFeedback("搭档成功，换一个任务继续。" ); } } return <div className="system-pairing"><h2>任务：{task.label}</h2><div>{ITEMS.map((item) => <button aria-pressed={selected.includes(item.id)} key={item.id} onClick={() => setSelected((items) => items.includes(item.id) ? items.filter((id) => id !== item.id) : [...items, item.id])} type="button"><span>{item.icon}</span><strong>{item.label}</strong><small>{classifySystemItem(item.id) === "hardware" ? "硬件" : "软件"}</small></button>)}</div><p role="status">{feedback}</p><button className="primary-action" onClick={check} type="button">启动这组搭档</button></div>; }
