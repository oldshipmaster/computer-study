import { GRID_OBSTACLES, GRID_SIZE, GRID_TARGETS, coordinateKey, type GridDirection, type GridState } from "@/lib/grid-city-lesson";
interface Props { state: GridState; onMove: (direction: GridDirection) => void; }
export function GridCityGame({ state, onMove }: Props) {
  const cells = Array.from({ length: GRID_SIZE * GRID_SIZE }, (_, index) => ({ row: Math.floor(index / GRID_SIZE) + 1, col: index % GRID_SIZE + 1 }));
  return <div className="grid-city-game"><div className="grid-label-row" aria-hidden="true">{Array.from({ length: 6 }, (_, i) => <span key={i}>列{i + 1}</span>)}</div><div className="grid-city" role="grid" aria-label="六行六列的方格城">{cells.map((cell) => { const key = coordinateKey(cell); const robot = state.position.row === cell.row && state.position.col === cell.col; return <div aria-label={`第${cell.row}行第${cell.col}列${GRID_OBSTACLES.includes(key) ? "，障碍" : GRID_TARGETS.includes(key) ? "，坐标信标" : ""}${robot ? "，机器人在这里" : ""}`} className={`grid-cell ${GRID_OBSTACLES.includes(key) ? "is-obstacle" : ""} ${state.visitedTargets.includes(key) ? "is-visited" : ""}`} key={key} role="gridcell"><small>{cell.row},{cell.col}</small><span>{robot ? "🤖" : GRID_OBSTACLES.includes(key) ? "🪨" : GRID_TARGETS.includes(key) ? "⭐" : ""}</span></div>; })}</div><div className="grid-controls" aria-label="机器人方向控制"><button onClick={() => onMove("up")} type="button">↑ 上</button><button onClick={() => onMove("left")} type="button">← 左</button><button onClick={() => onMove("down")} type="button">↓ 下</button><button onClick={() => onMove("right")} type="button">→ 右</button></div><p role="status">{state.feedback} 已找到 {state.visitedTargets.length}/3 个信标。</p></div>;
}
