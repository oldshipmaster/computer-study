export interface TransferState { local: { exists: boolean; version: number }; cloud: { exists: boolean; version: number }; sharedWith: "none" | "family"; feedback: string; }
export type TransferAction = { type: "download" | "upload" | "sync" | "editLocal" } | { type: "share"; audience: "family" };
export const INITIAL_TRANSFER_STATE: TransferState = { local: { exists: false, version: 0 }, cloud: { exists: true, version: 1 }, sharedWith: "none", feedback: "云端有第 1 版虚构作品。" };
export function updateTransfer(state: TransferState, action: TransferAction): TransferState { if (action.type === "download" && state.cloud.exists) return { ...state, local: { ...state.cloud }, feedback: "下载创建了本机副本，云端副本仍然存在。" }; if (action.type === "editLocal" && state.local.exists) return { ...state, local: { exists: true, version: state.local.version + 1 }, feedback: "只修改了本机副本。" }; if (action.type === "upload" && state.local.exists) return { ...state, cloud: { ...state.local }, feedback: "上传把本机版本送到云端。" }; if (action.type === "sync" && state.local.exists) { const version = Math.max(state.local.version, state.cloud.version); return { ...state, local: { exists: true, version }, cloud: { exists: true, version }, feedback: "同步让两个位置的版本保持一致。" }; } if (action.type === "share") return { ...state, sharedWith: action.audience, feedback: "共享改变了谁能访问云端副本，没有搬走文件。" }; return state; }
