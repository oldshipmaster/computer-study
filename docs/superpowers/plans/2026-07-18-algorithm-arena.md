# 算法竞技场实施计划

1. 先写失败测试，固定六关数据、状态转换、错误保留、双击保护、通关和轮换契约。
2. 在 `lib/algorithm-arena-game.ts` 实现纯关卡目录与状态机。
3. 新建按需加载的 `AlgorithmArenaGame` 组件和独立样式，接入五课解锁、可视步骤、反馈、焦点管理和重玩。
4. 在 `IslandMap` 和统一游戏中心登记第八种玩法，并补齐渲染与组件契约。
5. 更新 README 隐私/课程说明，执行目标测试和完整发布门禁。
6. 提交并推送 `main`，等待 GitHub Pages 成功后验证页面、manifest、独立 JS/CSS 与关键文案。
