# 机器人代码远征实施计划

1. 先为纯函数引擎编写失败测试：六关数据、指令编辑、展开执行、条件判断、碰撞、胜负和轮换。
2. 实现 `lib/robot-code-expedition.ts`，保证状态不可变、输入有界、关卡可验证。
3. 编写组件源码契约测试，约束解锁、地图、程序队列、逐步执行、失败修复、重玩和可访问性。
4. 实现懒加载的 `RobotCodeExpedition.tsx` 与独立 CSS，并接入 `IslandMap`。
5. 更新渲染、Pages 资源和 README 隐私说明。
6. 运行完整发布门禁，提交并推送 `main`，等待 GitHub Pages 后验证线上 HTML、资源清单和懒加载脚本。
